---
name: ui-ux-pro-max
description: >-
  ALIAS → utilise le méga-skill `/design`. Ancien nom du pack UI/UX animé,
  conservé pour compatibilité. Toute demande de design / UI / UX / frontend /
  animation / landing / composant doit passer par `/design`, qui réunit tout
  (direction esthétique, Framer Motion + presets, 21st.dev, performance,
  accessibilité, branding Orange/IDRAC/signature).
---

# ui-ux-pro-max → alias de `/design`

Ce skill a été **fusionné** dans le méga-skill **`/design`** (2026-06-07).

➡️ **Invoque `/design`** (skill `design`) à la place. Il contient :
- l'**Étape 0 branding** (Orange reste brandé, IDRAC charté, sinon style signature) ;
- la direction esthétique anti « AI slop » ;
- le moteur Framer Motion + `patterns/framer-motion-presets.tsx` ;
- l'intégration 21st.dev (Magic) ;
- la performance (Core Web Vitals) en pilier central + l'accessibilité.

Ressources déplacées sous `.claude/skills/design/` :
`branding/BRANDS.md`, `styles/signature-dark-animated.md`,
`patterns/framer-motion-presets.tsx`, `patterns/PATTERNS.md`.

> L'**agent** `ui-ux-pro-max` (sous-agent pour gros chantiers UI) reste valide et
> applique désormais les mêmes règles que `/design`.
