# Local TTS server — default (Piper) dependencies. M.E.R.L.I.N.
#
# Piper is the default engine: fast on CPU, French voices, NO torch needed.
# This is all you need for the recommended CPU-only deployment.
#
# For the optional VoxCPM (neural voice cloning, GPU-oriented) engine, also
# install requirements-voxcpm.txt — see docs/VOXCPM_DEPLOYMENT.md.

piper-tts>=1.2.0
fastapi>=0.110
uvicorn>=0.29
soundfile>=0.12
numpy>=1.24
