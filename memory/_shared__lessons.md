| 2026-08-11 | agent-run écrivait son état dans `<state_dir>/<id>.json`, écrasé par les données du même agent (llm-bench, billing) | État isolé dans `<state_dir>/state/<id>.json` ; le portail lit le nouvel emplacement avec repli sur l'ancien | Session VM v9 |
| 2026-08-11 | Détection « CI de nouveau verte » par grep sur le JSON brut : cassée dès que l'écriture est passée à json.dumps (espace après `:`) → notification à chaque build vert | Lecture en JSON, tolérante aux deux formats, silence si l'état précédent est inconnu | Session VM v9 |
| 2026-08-11 | Bench LLM : « champion = le plus lent sous 60 s » élit un petit modèle ; un gros peut être plus rapide au mur car il génère moins de tokens | Critère = le plus GROS modèle sous le budget (taille lue dans le tag) | Session VM v7 |
| 2026-08-11 | SW PWA cache-first sur du code non fingerprinté : après déploiement, le navigateur garde l'ancien studio.js POUR TOUJOURS (Décider invisible, voyants vides) | SW v2 network-first pour code/CSS + cache-busting `?v=<démarrage>` injecté par Flask — un worker périmé ne peut pas servir une URL inconnue | Session v12 |
| 2026-08-11 | UI avec identifiants techniques (gd-content-gap, MOYEN, sha) : illisible pour l'utilisateur — « on dirait un autre projet » | Tout texte visible en français narratif : noms d'agents traduits, kinds traduits, messages de commit décapés de leurs préfixes | Session v12 |
| 2026-08-11 | Une clé privée d'API Oracle a été collée dans une conversation (recherche de clé SSH mal ciblée : mon motif « BEGIN PRIVATE KEY » matchait aussi ~/.oci) | Rotation immédiate (nouvelle clé + suppression de la compromise). RÈGLE : ne jamais demander ni accepter une clé privée dans un échange — seules les clés PUBLIQUES et les empreintes se partagent. Toute recherche de clé doit exclure .oci et n'afficher que des CHEMINS | Session v13 |
| 2026-08-11 | Root inaccessible sur la VM : ocarun sans sudo, SSH bloqué en sortie (PC Orange ET sandbox), user_data non modifiable, plugins agent limités à Run Command | La VM peut se joindre ELLE-MÊME : déposer la clé SSH via Run Command (canal chiffré, jamais le chat) → `ssh opc@127.0.0.1 "sudo ..."` → effacer la clé dans la même commande. Disque 30→83 Go | Session v14 |
| 2026-08-11 | Recherche de clé privée par motif « BEGIN PRIVATE KEY » sans exclure ~/.oci : a désigné la clé d'API du tenancy, que Maxime a collée dans le chat → rotation d'urgence | Toujours exclure .oci/.aws/.ssh des recherches génériques ; ne jamais demander un fichier dont le contenu est un secret — demander l'empreinte, jamais le contenu | Session v14 |
| 2026-08-15 | `RenderingServer.frame_post_draw` ne se declenche JAMAIS en headless : `MerlinNative._boot()` l'attendait, restait suspendu sans erreur, et AUCUN outil headless (smoke, soak, CI) n'a jamais charge le modele — tous validaient le jeu sur ses banques de secours | En headless : refus explicite (`model_failed` + `boot_error()` interrogeable) au lieu d'une attente muette ; opt-in `MERLIN_ALLOW_HEADLESS_LLM`. REGLE : un composant qui renonce doit le DIRE — une attente eternelle se fait passer pour de la lenteur | Session v22 |
| 2026-08-15 | Un signal (`model_failed`) ecoute trop tard ne sera jamais recu : `_boot` part en differe des le demarrage. La sonde a attendu 300 s un evenement deja passe | Doubler tout signal d'un ETAT interrogeable pour les retardataires. REGLE : signal = notification, jamais source de verite | Session v22 |
| 2026-08-15 | Conclusion tiree d'une moyenne de charge lue 5 s apres avoir tue le processus (encore en decroissance) → « la VM est surchargee », faux. Idem « l'auto-synchro ne marche pas », dementi par 415 passages au journal | Lire une metrique APRES stabilisation, et verifier une hypothese sur tout l'historique avant de conclure. REGLE : mesurer, puis conclure — jamais l'inverse | Session v23 |
| 2026-08-15 | `pkill -f "motif"` dans un `bash -c` : le TEXTE du script est sa propre ligne de commande, le motif se trouve lui-meme et tue le shell appelant (rc=143, travail fait mais derniere ligne jamais atteinte → un agent qui reussit en se signalant en echec) | Motif entre crochets (`[a]pp.py`), comme le `grep [c]hose` classique | Session v23 |
| 2026-08-15 | `install-agents.sh` fait `chmod +x` sur les scripts : un agent arrive non executable depuis GitHub devient « modifie » sans une ligne de difference, et un garde-fou anti-ecrasement bloque alors la synchro POUR TOUJOURS — l'agent s'auto-condamnait a son deploiement | Bit executable porte par le depot + comparaison avec `core.fileMode=false` (on ne regarde que le CONTENU) | Session v23 |
| 2026-08-15 | Le mythe « ~1 tok/s » (justifiant toutes les banques de secours) n'avait jamais ete mesure sur le moteur natif : mesure reelle 2,93 puis 4,31 tok/s apres reglage des fils. Et la chaine d'echantillonnage C++ finit par `greedy` inconditionnel → tirage deterministe, `temperature`/`top_p` inertes depuis toujours | Mesurer avant d'architecturer autour d'un chiffre herite. Un chiffre non date et non source est une rumeur | Session v23 |
| 2026-08-18 | `--quit-after` de Godot compte des IMAGES, pas des secondes. Un test lance avec « 200 » en croyant dire 200 s a rendu **66 s** de vie sur la VM (rendu logiciel llvmpipe ≈ 3 images/s, et l'ecran de selection descend volontairement a 5). La generation a ete coupee en plein vol sous TOUS les filets (renoncement 120 s, delai moteur 150 s) → aucune trace, deux tours de diagnostic perdus a chercher un bug qui n'existait pas | Borner un test a l'HORLOGE (`MERLIN_QUIT_AFTER_S` + chien de garde `sleep N; kill -TERM $$` arme avant le `exec`). REGLE : un budget d'images ne borne pas un temps — il varie d'un facteur dix selon la charge | Session v30 |
| 2026-08-18 | `inner.log` ne consignait pas la ligne de commande de Godot, et l'ecran de selection ne parlait QUE lorsqu'il renoncait : une reussite etait muette, donc indistinguable d'un ecran jamais ouvert. Impossible de prouver ce qui avait ete lance | Consigner la ligne de commande exacte avant tout `exec`, et faire ecrire un TEMOIN d'ouverture + la duree a la reussite. REGLE : un test qui ne dit pas ce qu'il a lance ne prouve rien | Session v30 |
| 2026-08-18 | « Ollama est 2× plus rapide que le moteur du jeu » (4,31 contre 8,60 tok/s) a oriente le diagnostic pendant des jours. Mesure sur LE MEME prompt : Ollama 14,3 s de lecture + 16,5 s d ecriture, le jeu 15,9 s + 16,5 s -- EGALITE. L ancien chiffre du jeu divisait les tokens par le temps TOTAL (lecture du prompt incluse), celui d Ollama comptait l ecriture seule | Ne jamais comparer deux vitesses sans verifier qu elles mesurent la meme chose. REGLE : un tok/s sans indication de ce qu il inclut est inexploitable | Session v30 |
| 2026-08-18 | Le GGUF du jeu, nomme `gemma4-e4b-q4_k_m.gguf`, est un LIEN SYMBOLIQUE vers le blob d Ollama : memes poids, quantification Q4_0 et non Q4_K_M. Le nom a fait naitre deux hypotheses fausses (quantification differente, build a optimiser) | Verifier ce qu un fichier EST, pas ce que son nom annonce. Un `ls -l` aurait suffi et aurait economise deux pistes | Session v30 |
| 2026-08-18 | La moitie du cout d une generation etait la RELECTURE du prompt (655 tokens, 26,9 s en jeu), reevalue integralement a chaque appel : la reutilisation du cache KV existait mais avait ete desactivee pour un bug reel (le traqueur n enregistrait pas les tokens ECRITS, seulement ceux du prompt) | Une optimisation desactivee « temporairement » pour un bug se paie a chaque appel, indefiniment. La reparer coutait une ligne | Session v30 |
| 2026-08-18 | En optimisant l ordre du prompt pour le cache, j ai repousse la consigne de format en avant-derniere position. Le modele a repondu en bloc markdown, s est fait couper au plafond, et l ecran a relance une seconde generation : 115 s au lieu de 69 | La derniere position d un prompt appartient a la consigne de FORMAT. Un gain de vitesse qui declenche un second essai est une perte | Session v30 |
| 2026-08-18 | Le prompt d issue pesait ~1000 tokens et son EVALUATION seule coutait 52-53 s (ecriture : 9 s) -- plus que la fenetre d attente entiere. Le secours etait servi a chaque beat alors que le modele ecrivait bel et bien | Ordonner tout prompt recurrent en TETE STABLE (regles, exemple -- lues une fois grace au cache de prefixe) / QUEUE VARIABLE (scene, degre). Les consignes de la tete doivent etre DEGRE-NEUTRES pour rester identiques d un appel a l autre | Session v30 |
| 2026-08-18 | Sur le moteur mono-place, l arc « en arriere-plan » entre les beats evincait le cache du prompt d issue ET brulait ses essais sur les annulations (la resolution passe devant a chaque pose) : credit epuise en deux beats, fin de quete au secours | Pour un travail de fond sur ressource unique : budget a l HORLOGE, jamais un compte d essais -- une collision n est pas un echec. Et comparer les harnais au rythme HUMAIN : une pose de 8 s jouait plus vite qu aucun joueur reel | Session v30 |
| 2026-08-18 | La revue adversariale du diff (3 lentilles + refutation) a confirme 4 defauts reels dont un critique (course de cadence : le jeu restait a 5 fps pour la session) -- tous dans mon propre diff, aucun detectable par parse check | Sur du code de concurrence/timing, la revue adversariale AVANT deploiement n est pas un luxe : chaque trouvaille confirmee aurait coute un tour de diagnostic VM d une heure | Session v30 |
| 2026-08-19 | n_ctx 4096 (la valeur de la bible R58) fait s effondrer llama_decode en segfault (graph_compute) sur ce build ARM avec Gemma 4 : l attention a fenetre glissante emprunte un autre chemin a cette taille. Le « prompt de 2582 tokens » qui avait motive l agrandissement etait un mensonge d une course de compteurs (reset sur le fil principal pendant qu un fil d inference vivait encore) | Un chiffre impossible (2582 > n_ctx) est un chiffre qui MENT : verifier l instrumentation avant d agir dessus. Et 2048 est prouve stable -- monter llama.cpp de version avant de retoucher cette constante | Session v30-nuit |
| 2026-08-19 | Le lookahead (scene N+1 ecrite en connaissant l issue N) etait tue par TROIS mecanismes successifs : lance a l affichage il n avait que le temps de lecture ; invalidate_resolution l annulait a chaque presentation de beat ; et au rythme du harnais (18 s) une scene de ~40 s ne peut de toute facon pas retomber a temps | Sur un moteur mono-place, chaque consommateur doit declarer sa priorite ET sa fenetre. Et un harnais qui joue plus vite qu un humain mesure un jeu que personne ne vit -- calibrer les pauses sur le rythme de LECTURE reel | Session v30-nuit |
| 2026-08-19 | Laboratoire e2b/e4b sur contexte fige : e2b est 35-46 % plus rapide PARTOUT et ecrit autant, mais il PERD LE FIL -- sa scene lookahead ignore le chevalier, l enfant et le sentier de l issue precedente, la ou e4b les porte tous | La vitesse d un modele ne se juge pas sans juger sa MEMOIRE du contexte : le critere de l enchainement (porter la trace de ce qui vient d etre fait) discrimine mieux que la qualite de prose. e4b = cerveau du recit ; e2b = candidat cerveau vif (issues, intro) pour une architecture bi-cerveaux | Session v30-nuit |
| 2026-08-19 | Labo bi-cerveaux : intro a 131,9 s (ecriture 1,77 tok/s au lieu de 9,5 sur le meme Vif) -- le braséro de 08:00 a recharge e4b dans Ollama EN PLEIN labo, car gates.py ne regardait que le port VNC 5900 : un Godot headless etait invisible pour lui | Toute charge LLM lourde doit etre visible du gate, pas seulement la session VNC : gates.py compte desormais tout godot --headless --script comme « jeu en cours ». Et une ecriture qui s effondre a mi-generation avec un eval rapide = concurrence EXTERNE, pas un probleme de routage | Session v31 |
