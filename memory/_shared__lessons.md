| 2026-08-11 | agent-run écrivait son état dans `<state_dir>/<id>.json`, écrasé par les données du même agent (llm-bench, billing) | État isolé dans `<state_dir>/state/<id>.json` ; le portail lit le nouvel emplacement avec repli sur l'ancien | Session VM v9 |
| 2026-08-11 | Détection « CI de nouveau verte » par grep sur le JSON brut : cassée dès que l'écriture est passée à json.dumps (espace après `:`) → notification à chaque build vert | Lecture en JSON, tolérante aux deux formats, silence si l'état précédent est inconnu | Session VM v9 |
| 2026-08-11 | Bench LLM : « champion = le plus lent sous 60 s » élit un petit modèle ; un gros peut être plus rapide au mur car il génère moins de tokens | Critère = le plus GROS modèle sous le budget (taille lue dans le tag) | Session VM v7 |
