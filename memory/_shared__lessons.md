| 2026-08-11 | agent-run écrivait son état dans `<state_dir>/<id>.json`, écrasé par les données du même agent (llm-bench, billing) | État isolé dans `<state_dir>/state/<id>.json` ; le portail lit le nouvel emplacement avec repli sur l'ancien | Session VM v9 |
| 2026-08-11 | Détection « CI de nouveau verte » par grep sur le JSON brut : cassée dès que l'écriture est passée à json.dumps (espace après `:`) → notification à chaque build vert | Lecture en JSON, tolérante aux deux formats, silence si l'état précédent est inconnu | Session VM v9 |
| 2026-08-11 | Bench LLM : « champion = le plus lent sous 60 s » élit un petit modèle ; un gros peut être plus rapide au mur car il génère moins de tokens | Critère = le plus GROS modèle sous le budget (taille lue dans le tag) | Session VM v7 |
| 2026-08-11 | SW PWA cache-first sur du code non fingerprinté : après déploiement, le navigateur garde l'ancien studio.js POUR TOUJOURS (Décider invisible, voyants vides) | SW v2 network-first pour code/CSS + cache-busting `?v=<démarrage>` injecté par Flask — un worker périmé ne peut pas servir une URL inconnue | Session v12 |
| 2026-08-11 | UI avec identifiants techniques (gd-content-gap, MOYEN, sha) : illisible pour l'utilisateur — « on dirait un autre projet » | Tout texte visible en français narratif : noms d'agents traduits, kinds traduits, messages de commit décapés de leurs préfixes | Session v12 |
| 2026-08-11 | Une clé privée d'API Oracle a été collée dans une conversation (recherche de clé SSH mal ciblée : mon motif « BEGIN PRIVATE KEY » matchait aussi ~/.oci) | Rotation immédiate (nouvelle clé + suppression de la compromise). RÈGLE : ne jamais demander ni accepter une clé privée dans un échange — seules les clés PUBLIQUES et les empreintes se partagent. Toute recherche de clé doit exclure .oci et n'afficher que des CHEMINS | Session v13 |
| 2026-08-11 | Root inaccessible sur la VM : ocarun sans sudo, SSH bloqué en sortie (PC Orange ET sandbox), user_data non modifiable, plugins agent limités à Run Command | La VM peut se joindre ELLE-MÊME : déposer la clé SSH via Run Command (canal chiffré, jamais le chat) → `ssh opc@127.0.0.1 "sudo ..."` → effacer la clé dans la même commande. Disque 30→83 Go | Session v14 |
| 2026-08-11 | Recherche de clé privée par motif « BEGIN PRIVATE KEY » sans exclure ~/.oci : a désigné la clé d'API du tenancy, que Maxime a collée dans le chat → rotation d'urgence | Toujours exclure .oci/.aws/.ssh des recherches génériques ; ne jamais demander un fichier dont le contenu est un secret — demander l'empreinte, jamais le contenu | Session v14 |
