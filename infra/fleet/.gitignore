# Secrets & local state — never commit
**/terraform.tfvars
**/*.tfstate
**/*.tfstate.*
**/.terraform/
**/tfplan
# Generated fleet status
status/
# Cloudflare / node tooling
cloudflare/.wrangler/
**/node_modules/
# GCP service-account keys
**/*.json.key
*-sa.json
fleet.local.yaml
